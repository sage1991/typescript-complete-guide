{
  "scripts": {
    "start:server": "parcel ./index.html",
    "start:db": "json-server -w db.json",
    "start": "concurrently npm:start:*"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "json-server": "^0.16.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "axios": "^0.20.0"
  }
}
